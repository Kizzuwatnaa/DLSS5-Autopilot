# Windows version resource for the built executable.
#
# An .exe with no company, product or version at all is one of the signals
# machine-learning antivirus models weigh, and every PyInstaller build ships
# without one by default. Filling it in is free, honest, and makes the file
# look like what it is rather than like an anonymous binary.
#
# Keep the version numbers here in step with core/update.py VERSION.
VSVersionInfo(
  ffi=FixedFileInfo(
    filevers=(1, 3, 2, 0),
    prodvers=(1, 3, 2, 0),
    mask=0x3f,
    flags=0x0,
    OS=0x40004,
    fileType=0x1,
    subtype=0x0,
    date=(0, 0)
  ),
  kids=[
    StringFileInfo([
      StringTable(
        '040904B0',
        [StringStruct('CompanyName', 'Kizzuwatnaa'),
         StringStruct('FileDescription', 'DLSS 5 Autopilot - installs DLSS 5 neural rendering into your games'),
         StringStruct('FileVersion', '1.6.0.0'),
         StringStruct('InternalName', 'dlss5-autopilot'),
         StringStruct('LegalCopyright', 'MIT licensed. Not affiliated with NVIDIA.'),
         StringStruct('OriginalFilename', 'dlss5-autopilot.exe'),
         StringStruct('ProductName', 'DLSS 5 Autopilot'),
         StringStruct('ProductVersion', '1.6.0.0'),
         StringStruct('Comments', 'https://github.com/Kizzuwatnaa/DLSS5-Autopilot')])
    ]),
    VarFileInfo([VarStruct('Translation', [1033, 1200])])
  ]
)
