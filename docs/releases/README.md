# Release notes

One file per release, named after its tag. The release page on GitHub is
filled from the file matching the tag being built, so the name is
load-bearing: `v1.7.3.md` becomes the body of `v1.7.3`.
