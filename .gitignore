# Build output
dlss5-autopilot.exe
dlss5kur.exe
build/
dist/
*.spec
__pycache__/
*.pyc

# The DLSS 5 add-on and NVIDIA runtimes are proprietary, unlicensed software.
# They are NEVER committed here; the tool downloads them at run time.
renodx/
*.addon64
*.addon32
*.dll

# Local settings
settings.json

dist-new/

# The detection baseline is one machine's game library - the names and
# executables of whatever is installed on it. That is personal, it is only
# meaningful on the machine that wrote it, and _tools/detect_check.py
# writes it with --save on first run.
_tools/detect_baseline.json
